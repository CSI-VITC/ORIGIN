- When requesting a narrowly scoped change, prefers that only the specified element be changed and everything else remain untouched. Confidence: 0.9
- Prefers working on original source files rather than compiled/minified bundles (e.g., avoid editing `public/_nuxt/entry.*.js`; edit `app/page.js`, `.vue` components, or the underlying data source — JSON/Markdown/CMS — instead). Willing to edit compiled bundles ONLY when the source files genuinely don't exist in the repo and the bundle is confirmed to be the authoritative source; in that case edits should be narrowly scoped to the affected section. Confidence: 0.95
- When specific real-world data (names, titles, dates, etc.) is not yet available, prefers explicit placeholder tokens like `[Faculty Name]`, `[Coordinator Name]`, `[Date]` rather than invented/fabricated values. Confidence: 0.95
- Before making destructive/irreversible changes (especially to compiled artifacts, bundles, SVGs, or any file without a version-control history), wants a backup copy (e.g., `.bak` sibling, `.backup/` folder) created first so edits are reversible. Confidence: 0.95
- When replacing content inside an existing UI component, prefers pure content swaps that preserve all existing structure — layout, animations (swiper/parallax/scroll), card sizes, spacing, transforms, responsiveness, typography, and styling must remain untouched; only the strings/text/markup content changes. Confidence: 0.95
- All requested elements must be changed in a single response — does not accept "I'll do the rest next turn" / "patched card 1, will do cards 2–4 later" / "inspected but not patched yet" patterns. When the user says "ALL OTHER CARDS", "EVERY ONE", etc., every targeted item must be fully edited before reporting back. Confidence: 0.9
- Wants the response to report a complete, verifiable end-state (file actually edited, syntax validated, ready for the user to hard-refresh and see) — not an intermediate inspection report. "I've located the bytes" / "plan is X, executing next" without the change actually being written and validated is treated as a failure. Confidence: 0.85
- When multiple scripted tools/runs are needed, prefers running them in a single chained pass (write script → run → verify) within one turn rather than many back-and-forth tool calls, to avoid leaving the file in a partial/corrupt intermediate state. Confidence: 0.7
- Communicates tersely and expects terse progress reports — short status messages ("6 slides ✓", "syntax OK", "all checks pass") rather than lengthy narrative explanations of what was tried. Short exclamations like "ayoo wtf" or "the issue is still there" signal visible breakage in the user-facing output, so any reported success must be backed by real syntax/DOM verification, not optimistic assumptions. A local `node --check` pass is NOT sufficient — must also clear dev-server/browser caches (`.next/cache`, hard-refresh), and verify the bundle actually served to the browser matches the file on disk before declaring it fixed. Confidence: 0.9
- Strongly prefers CSS-based styling over embedding presentation markup such as inline `<span style="...">` in content strings; literal HTML must never leak into the rendered UI. Confidence: 0.95
- Expects edits that remove accidental markup to be narrowly targeted: strip only the unintended wrappers while preserving legitimate JSX/HTML spans elsewhere. Confidence: 0.9
- When auditing visual/readability problems, wants the entire visible surface scanned — every instance of unreadable text (header nav, mobile menu, footer, body copy, typography utility classes, etc.) addressed in one pass with a consistent readable color (typically white), not piecemeal one-element-at-a-time fixes. Confidence: 0.85
- When fixing visibility/readability issues, prefers a single well-scoped CSS override block at the end of the global stylesheet using `!important` on the affected selectors (typography utilities, nav/menu/header/footer) plus a body/anchor color flip, rather than touching individual JSX components — keeps the fix centralized and layout/animations untouched. Confidence: 0.8
- Writes in casual, sometimes typo-laden lowercase ("thats", missing apostrophes) and uses blunt exclamations when something visibly breaks ("ayooo wtf is this text"); expects the assistant to interpret intent, not literal text. Confidence: 0.75
